<h1 class="text-center mt-4">Edit tag name</h1>
<div class="container">
  <div class="row col-12 justify-content-center">
    <div class="col-10">
      <%= render 'form', locals: {name: "Edit"} %>
    </div>
    <div class="row m-auto mb-3">
      <%= link_to '[ Cancel and return to tag ]', category_path(@category), class: "text-info" %>
    </div>
  </div>
</div>